# JARVIS X2 — P3 Intelligence Core

P3 connects the P0/P1/P2 experience to real local intelligence infrastructure.

## Included
- Hermes Runs API adapter
- Hermes SSE event translation
- stop + approval support
- Hermes Jobs API adapter
- Graphiti temporal-memory local REST service
- Graphiti adapter
- JARVIS Hermes memory plugin
- local Ollama config example
- Home Assistant server adapter
- n8n webhook adapter
- safe Browser Worker boundary
- tool registry + policy engine
- delegation UI
- reasoning/status UI
- intelligence lab
- server route templates
- free/local stack guide
- Claude Code integration guide + master prompt

## Deliberately not faked
Browser Use execution is not switched on automatically.
It requires version pinning, sandboxing and policy tests first.

## Primary goal
A complete local loop:
voice/text → Hermes → memory/tools/subagents → approval → result → optional speech.
